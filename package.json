{
  "name": "setup-minikube",
  "version": "1.0.0",
  "description": "GitHub Action to install and configure minikube with automatic cleanup",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "format": "prettier --write 'src/**/*.ts'",
    "lint": "eslint src/**/*.ts",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fenio/setup-minikube.git"
  },
  "keywords": [
    "actions",
    "kubernetes",
    "minikube",
    "ci",
    "cd"
  ],
  "author": "fenio",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.9.3"
  }
}
